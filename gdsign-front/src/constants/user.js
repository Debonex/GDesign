const login = {
  success: "user.login.success",
  failed: "user.login.failed"
};

const register = {
  success: "user.register.success",
  failed: "user.register.failed",
  duplicate: {
    uid: "user.register.duplicate.uid"
  }
};

export default {
  login,
  register
};
